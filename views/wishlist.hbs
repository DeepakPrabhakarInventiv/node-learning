{{> header }}

<div class="bg-white">
    <div class="container py-5">

        <h3 class="border-bottom border-dark pb-2 mb-5">Wishlist</h3>

        <!-- Begin::Products -->
        <div class="row">
            <div class="col-md-9">
                <div id="products">Loading...</div>
            </div>
            <div class="col-md-3" id="sidebar">
                <p>Wishlist Info:</p>
                <p>There are many variations of passages of Lorem Ipsum available, but the majority have suffered
                    alteration in some form, by injected humour, or randomised words which don't look even slightly
                    believable.</p>
            </div>
        </div>
        <!-- End::Products -->

    </div>
</div>

<!-- Toast Container -->
<div id="toast-container" class="toast-container position-fixed bottom-0 end-0 p-3"></div>


<script src="https://unpkg.com/axios@1.6.7/dist/axios.min.js"></script>
<script>
    window.addEventListener('load', function () {
        // Send form data to the server using axios
        const url = `/wishlist/products`;
        axios.post(url)
            .then(function (response) {
                console.log(response);
                document.getElementById('products').innerHTML = generateWishlist(response.data);
            })
            .catch(function (error) {
                document.getElementById('products').innerHTML = error.response.data.message;
                console.error(error);
            });
    });

    function generateWishlist(products) {
        let outputHtml = `<div id="cart-items">`;
        products.forEach(function (productItem, index) {
            const product = productItem.product_id;
            outputHtml += `<div class="card mb-3" id="${productItem._id}">
                                <div class="card-body">
                                    <h5>${product.name}</h5> 
                                    <p>Category: ${product.category}</p>
                                    <p>Price: ${product.price}</p>
                                </div>
                                <div class="card-footer">
                                    <form method="POST" class="form">
                                        <div class="row">
                                            <div class="col">
                                                <div class="update-cart d-flex">
                                                    <button type="button" name="update-cart-btn" class="update-cart-btn btn btn-outline-primary ms-2" data-cid="${productItem._id}" data-pid="${product._id}">Add to Cart</button>
                                                </div>
                                            </div>
                                            <div class="col text-end">
                                                <button type="button" class="remove-from-wishlist btn btn-outline-danger" data-cid="${productItem._id}">Remove</button>
                                            </div>
                                        </div>
                                    </form>
                                </div>
                            </div>`;
        });
        outputHtml += `</div>`;
        return outputHtml;
    }

    //update thr cart
    const container = document.getElementById('products');
    container.addEventListener('click', (event) => {
        if (event.target.classList.contains('update-cart-btn')) {
            const cid = event.target.getAttribute('data-cid');
            const pid = event.target.getAttribute('data-pid');
            const quantity = 1;
            //send it to API
            const formData = {
                'count': quantity,
                'product_id': pid,
            };

            const url = `/cart/`;
            axios.post(url, formData)
                .then(function (response) {
                    console.log(response);
                    createToast('Success', response.data.message, 'success');
                    document.getElementById(cid).remove();
                })
                .catch(function (error) {
                    console.error(error);
                    createToast('Error', error.response.data.message, 'danger');
                });
        }
    });

    //remove from the wishlist
    container.addEventListener('click', (event) => {
        if (event.target.classList.contains('remove-from-wishlist')) {
            const cid = event.target.getAttribute('data-cid');
            //send it to API
            const formData = {
                'id': cid
            };
            const url = `/wishlist/delete`;
            axios.post(url, formData)
                .then(function (response) {
                    console.log(response);
                    createToast('Success', response.data.message, 'success');
                    document.getElementById(cid).remove();

                })
                .catch(function (error) {
                    console.error(error);
                    createToast('Error', error.response.data.message, 'danger');
                });
        }
    });


    // Function to create and show a Bootstrap toast
    function createToast(title, message, type) {
        // Generate a unique ID for the toast
        const toastId = `toast-${Date.now()}`;

        // Toast template
        const toastHtml = `
        <div id="${toastId}" class="toast align-items-center text-bg-${type} border-0" role="alert" aria-live="assertive" aria-atomic="true">
          <div class="d-flex" data-bs-theme="dark">
            <div class="toast-body" >
              <strong>${title}:</strong> ${message}
            </div>
            <button type="button" class="btn-close me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
          </div>
        </div>
      `;

        // Create a wrapper element and set its inner HTML
        const wrapper = document.createElement('div');
        wrapper.innerHTML = toastHtml.trim();

        // Append the toast to the container
        const toastContainer = document.getElementById('toast-container');
        toastContainer.appendChild(wrapper.firstChild);

        // Initialize and show the toast
        const toastElement = document.getElementById(toastId);
        const toast = new bootstrap.Toast(toastElement, { delay: 5000 }); // 5-second delay
        toast.show();

        // Remove the toast from the DOM after it hides
        toastElement.addEventListener('hidden.bs.toast', function () {
            toastElement.remove();
        });
    }
</script>

{{> footer }}